;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list nil)

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 24)
        (left . 65)
        (width . 228)
        (height . 70)))

