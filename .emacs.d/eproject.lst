;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list nil)

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 14)
        (left . 982)
        (width . 112)
        (height . 67)))

